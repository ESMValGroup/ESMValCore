---
name: esmvaltool
channels:
  - conda-forge

dependencies:
  - compilers
  - esmpy
  - iris>=2.2.1
  - graphviz
  # - libunwind  # Needed for Python3.7+ TEST for replacing vmprof with vprof
  - python>=3.6  # if 3.7 lxml will not import correctly if <4.5.0
  - python-stratify
