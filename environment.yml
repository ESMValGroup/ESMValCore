---
name: esmvaltool
channels:
  - conda-forge

dependencies:
  - esmpy
  - iris>=2.2.1
  - graphviz
  - gcc_linux-64
  - libunwind  # Needed for Python3.7+
  - python>=3.6  # if 3.7 lxml will not import correctly if <4.5.0
  - python-stratify
