# Site-specific entries: BSC
# Edit the lines below to locate data at BSC
---

CMIP5:
  data:
    - input_dir: '{type}/{project}/{exp}/{dataset.lower}'
OBS:
  data:
    - input_dir: '{type}/{institute.lower}/{dataset.lower}/{freq_folder}/{short_name}{freq_base}'
      input_file: '{short_name}_*.nc'
OBS6:
  data:
    - input_dir: '{type}/{institute.lower}/{dataset.lower}/{freq_folder}/{short_name}{freq_base}'
      input_file: '{short_name}_*.nc'
