"""CMORizer for certain projects."""
import logging

logger = logging.getLogger(__name__)


def cmorize(in_file, short_name, cmorizer, var_mapping, output_dir):
    """Use project-specific CMORizer and CMORizer data."""
    pass
